
閉じタグが必要なタグ

<h1>見出し(h1~h6まであり、数が大きくなるほど見出しが小さくなる)</h1>
<p>段落(文章のまとまり)</p>
<a>リンク</a>

閉じタグが不要なタグ

<img>画像を表示する
<br>改行

――――――――――――――――――――――――――――――

タグの属性と値

属性は開始タグの要素名の後に、半角スペースで区切って書き入れ、
さらに、その程度を値で調整できる。

<a href = "https;//web-camp.io/">WebCamp１日目！</a>
　属性名　　　　属性値

a要素にhrefという属性をつけて、URLを指定する。
属性の後のURLがリンク先のURLで属性値と呼ぶ。
属性値は""で囲み、属性と属性値はイコールで繋ぐ。


・複数の属性を持たせる時

属性間で半角スペースを空けて、複数の属性を記述する事ができる。

<img src="sample.jpg" alt="サンプル画像">

src属性：画像の名前と場所
alt属性：画像が表示できないときに代替テキストとして表示されるテキスト


